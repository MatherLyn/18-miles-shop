export interface LoginConfig {
    username: string;
    password: string;
}

export interface RegisterConfig {
    username: string,
    nickname: string,
    phone: string,
    password: string,
    code: string
}